/**
 * Escrituras de pedidos (fase 2): crear pedido desde el checkout, firmar el
 * pago de Wompi, procesar el webhook de forma idempotente contra
 * `transaccion.wompi_id` y mover el estado del pedido.
 */
export {};
