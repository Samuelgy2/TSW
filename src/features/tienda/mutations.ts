/**
 * Escrituras del catálogo (fase 2): crear y editar productos y variantes,
 * subir imágenes al bucket `productos`, activar y desactivar.
 * Siempre con `crearClienteAdmin()` desde route handlers.
 */
export {};
