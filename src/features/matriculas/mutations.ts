/**
 * Escrituras de matrículas (fase 2): publicar documento, subir nueva versión
 * al bucket `documentos` y archivar la anterior.
 *
 * Todas usan `crearClienteAdmin()` desde route handlers y registran el hecho
 * en `evento_auditoria`.
 */
export {};
